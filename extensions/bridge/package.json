{
  "name": "@spsoft-mvp/extension-bridge",
  "version": "0.1.0",
  "description": "Bridges the OHIF viewer to the host app: accepts commands via postMessage and publishes viewer events back out.",
  "author": "spsoft-mvp",
  "license": "MIT",
  "main": "dist/spsoft-mvp-extension-bridge.umd.js",
  "module": "src/index.ts",
  "engines": {
    "node": ">=24"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test:unit": "jest --watchAll",
    "test:unit:ci": "jest --ci --runInBand --collectCoverage --passWithNoTests"
  },
  "peerDependencies": {
    "@ohif/core": "workspace:*",
    "@cornerstonejs/core": "5.10.3"
  }
}
